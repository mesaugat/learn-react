import React from 'react';

/**
 * Input component to add a new todo.
 */
const InputBox = () => {
  return (
    <input type="text" className="form-control add-todo" placeholder="Add new todo" />
  )
}

export default InputBox;
